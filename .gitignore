mphb-availability-calendar.zip
dcc-guest-guide.zip
# Owner deliverables, all regenerable from source (see CLAUDE.md):
# zips are 'Guest Guide <version>.zip'; every other file handed over
# carries the 'Guest Guide - ' prefix.
Guest Guide *.zip
Guest Guide - *
.DS_Store
node_modules/
