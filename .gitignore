mphb-availability-calendar.zip
dcc-guest-guide.zip
# Guest Guide deliverables are prefixed 'Guest Guide - ' (see CLAUDE.md).
# Build artifacts: regenerable from source, so not committed.
Guest Guide *.zip
# Anything handed to the owner carries the 'Guest Guide - ' prefix and is a
# build artifact (zip, screenshot, report), regenerable from source.
Guest Guide - *
.DS_Store
node_modules/
