<?php
/**
 * Uninstall handler. The plugin stores no options, transients, custom tables, or
 * client-side state, so there is nothing to clean up server-side.
 */

if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}
