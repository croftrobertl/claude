{
  "name": "dcc-cottage-selector-dev",
  "private": true,
  "version": "0.1.0",
  "description": "Dev tooling (DOM smoke tests) for the Dora Canal Cottage Selector plugin. Not shipped in the plugin zip.",
  "scripts": {
    "test": "php tools/build-bundle.php --check && node tools/build-min.js --check && php tools/build-css.php --check && php tools/css-lint.php && php tests/test-design-snapshot.php && node tests/dom-smoke.test.js",
    "build": "php tools/build-bundle.php && node tools/build-min.js && php tools/build-css.php"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "terser": "5.51.2"
  }
}
